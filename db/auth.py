from db import get_db_connection


def db_sign_up():
    # conn = get_db_connection()
    # # Open a cursor to perform database operations
    # cur = conn.cursor()
    #
    # cur.execute(query='INSERT INTO users(first_name, last_name, email, password, address, city, state, zip_code, phone)'
    #                   'VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)',
    #
    #
    #
    # conn.commit()
    #
    # cur.close()
    # conn.close()
    #
    # message = {'message': 'User successfully created!'}
    #
    # return message, 200
    pass

def db_login():
    pass


def db_logout():
    pass
